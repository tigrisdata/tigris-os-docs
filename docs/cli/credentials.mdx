# tigris credentials

Test whether your current credentials can reach Tigris and optionally verify
access to a specific bucket.

**Alias:** `creds`

## Usage

```bash
tigris credentials <operation> [flags]
t3 creds <operation> [flags]
```

## Examples

```bash
tigris credentials test
tigris credentials test --bucket my-bucket
```
