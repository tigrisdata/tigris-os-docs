# tigris login select

Choose how to login — OAuth (browser) or credentials (access key). This is the
default when running `tigris login` with no subcommand.

## Usage

```bash
tigris login select
t3 l select
```

## Examples

```bash
tigris login select
```
