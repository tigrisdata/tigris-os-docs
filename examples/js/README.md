# JavaScript examples for Tigris

## To run these tests locally

Grab the credentials from 1Password (look for "Tigris Examples" in the
Engineering vault) and copy the export statements into a terminal. Then run
`npm run examples`:

```text
npm run examples
```
